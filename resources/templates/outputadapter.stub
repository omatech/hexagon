<?php

namespace App\Application\${Domain}\${UseCase}${Folder};

use Illuminate\Http\JsonResponse;

final class ${UseCase}${Name} extends JsonResponse
{
    public static function ofSuccess(): ${UseCase}${Name}
    {
        return self::create([], 200);
    }

    public static function ofError(string $message, $errorCode = 400): ${UseCase}${Name}
    {
        return self::create(['message' => $message], $errorCode);
    }
}
