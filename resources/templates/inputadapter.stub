<?php

namespace App\Application\${Domain}\${UseCase}${Folder};

final class ${UseCase}${Name}
{

    // ${Attributes}

    public function __construct( /* ${AttributesWithTypes} */) {
        /* ${AttributeAssignation} */
    }

    // ${AttributeGetters}
}
